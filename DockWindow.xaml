<Window x:Class="MagicLittleBox.DockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:MagicLittleBox"
        mc:Ignorable="d"
        WindowStyle="None"
        ShowInTaskbar="False"
        Title="DockWindow" Height="450" Width="210"
        MinWidth="180">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0"
                Background="#FF262626"
                CornerRadius="4"
                Margin="4 4 4 0">
            <TextBox x:Name="LogTextBox"
                     Background="#DEDEDE"
                     BorderThickness="0"
                     FontFamily="Consolas"
                     FontSize="12"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     AcceptsReturn="True"
                     AcceptsTab="True"/>
        </Border>
        
        <Grid Grid.Row="1" Margin="4 0 4 0">
                
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
                
            <Button Grid.Column="0"
                    Style="{StaticResource QidongButton}"
                    Margin="1 5 3 5"
                    x:Name="RefreshButton"
                    >
                <TextBlock Text="刷新内容" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI" 
                           FontWeight="SemiBold"
                           FontSize="15"
                           Foreground="#E0E0E0"
                           Margin="3 0 0 1"/>
            </Button>
            <Button Grid.Column="1" 
                    Style="{StaticResource PauseButtonStyle}"
                    Margin="3 5 1 5"
                    x:Name="ClearButton"
                    Click="Clear">
                <TextBlock Text="清空信息" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI" 
                           FontWeight="SemiBold"
                           FontSize="15"
                           Foreground="#E0E0E0"
                           Margin="3 0 0 1"/>
            </Button>
                
        </Grid>
    </Grid>
</Window>
